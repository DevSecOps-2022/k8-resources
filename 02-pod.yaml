apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    environment: dev
    project: roboshop
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80