apiVersion: v1
kind: Pod
metadata:
  name: nginx-configmap-pod
spec:
  containers:
    - name: nginx
      image: nginx:latest
      envFrom:
        - configMapRef:
            name: nginx-configmap
